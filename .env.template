
# Project name used throughout the application
VITE_PROJECT_NAME="Qolca Solutions"

# Base URL for the backend API.
#  - DEV: leave empty so axios uses same-origin and Vite's dev proxy forwards
#    /api, /auth, /static, /media, /admin, /ws to the Docker backend
#    (default http://127.0.0.1:8000; override via VITE_BACKEND_PROXY_TARGET).
#  - PROD: set to the absolute API origin, e.g. https://api.example.com
VITE_API_URL=

# Optional — override the dev-proxy backend target (defaults to http://127.0.0.1:8000)
# VITE_BACKEND_PROXY_TARGET=http://127.0.0.1:8000

# Google Identity Services Web Client ID
# Obtain from Google Cloud Console -> Credentials -> OAuth 2.0 Client IDs
VITE_GOOGLE_CLIENT_ID=

# Izipay public key for the embedded payment form.
# Format: SHOP_ID:testpublickey_XXXXX (for TEST mode) or SHOP_ID:publickey_XXXXX (for PRODUCTION)
# Source: copy from Izipay back-office > Settings > Shop keys > Public key
# Reuses impresiones' shop ID 36933364 (eccomerce and impresiones share the same Izipay account,
# orders are disambiguated by order-number prefix: QLCA for eccomerce)
VITE_IZIPAY_PUBLIC_KEY=

# Google Maps JS API key with the Places library enabled.
# Optional — when unset, the AddressPicker on /checkout silently degrades
# to manual entry. Create + restrict in Google Cloud Console:
#   APIs allowed: Maps JavaScript API + Places API (New)
#   Referrers allowed: https://<your-domain>/* + http://localhost:5174/*
VITE_GOOGLE_MAPS_API_KEY=

# Display locale + currency (optional — defaults to es-PE / PEN).
# VITE_LOCALE=es-PE
# VITE_CURRENCY=PEN

# Sentry client-side error tracking (optional).
# VITE_SENTRY_DSN=
# VITE_SENTRY_ENVIRONMENT=local
# VITE_SENTRY_TRACES_SAMPLE_RATE=0.05
